//- player-info.pug
include ../partials/header.pug
include ../partials/menu.pug

div.container
  br
  div.row
    div.col-4
      div.card
        //- TODO: Modify the pug code below to conditionally set the image_src
        -if(result2)
          -var image_src = result2.img_src
        -else
          -var image_src = '../../resources/img/helmet.jpg'

        img.card-img-top#player_img(src= image_src, alt="Football Player")
        div.card-body.mx-auto
          form.form(action="/player_info/post", method="get")
            select.custom-select(name="player_choice", onchange="this.form.submit()")
              option(selected) Select Player
              //- TODO: Populate the select with player options. Set the value to the player's id and display the player's name.
    div.col-8
      div.table-responsive
        table.table
          //- TODO: Set the player's information. On select, fill in the player's information in each of the field below. You will need to calculate the average rushing, receiving, and passing yards.
          tr
            th Year
            -if(player_ID)
                td #{result2.year}
          tr
            th Games Played
            -if(player_ID)
                td #{gameCount}
          tr
            td Passing Yards:
            -if(player_ID)
                td Passing Yards: #{result2.passing_yards}
                td Avg. Passing Yards: #{Math.round(result2.passing_yards/gameCount)}
        tr
            th Major
            -if(player_ID)
                td #{result2.major}
        tr
          -if(player_ID)
              td Rushing Yards: #{result2.rushing_yards}
              td Avg. Rushing Yards: #{Math.round(result2.rushing_yards/gameCount)}
          tr
            -if(player_ID)
                td Receiving Yards: #{result2.receiving_yards}
                td Avg. Receiving Yards: #{Math.round(result2.receiving_yards/gameCount)}

include ../partials/footer.pug